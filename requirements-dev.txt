# Development dependencies (for local testing without Databricks)
pandas>=2.0.0,<3.0.0
streamlit>=1.28.0,<2.0.0
protobuf>=5.27.0,<6.0.0
pytest>=7.4.0,<8.0.0

# Optional: Install databricks packages only if needed
# databricks-sql-connector>=3.0.0,<4.0.0
# databricks-sdk>=0.18.0,<1.0.0
